/* Proposed class for Leaflet.draw handlers to inherit from. */
L.ToolbarAction = L.Handler.extend({
	options: {
		toolbarIcon: new L.ToolbarIcon(),
		children: []
	},

	/* Takes the map. */
	onAdd: function() {
		
	}
});